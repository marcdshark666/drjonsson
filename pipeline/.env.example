# Kopiera till .env (samma mapp). .env är gitignorad.
# Token: Printify > My profile > Connections > Generate API token
# (scopes: shops.read, catalog.read, uploads.write, products.write)
PRINTIFY_TOKEN=eyJ...
# Butikens id: python printify_bulk.py --shops
PRINTIFY_SHOP_ID=1234567
